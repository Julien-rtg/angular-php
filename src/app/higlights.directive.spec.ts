import { HiglightsDirective } from './higlights.directive';

describe('HiglightsDirective', () => {
  it('should create an instance', () => {
    const directive = new HiglightsDirective();
    expect(directive).toBeTruthy();
  });
});
